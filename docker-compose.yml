version: '3.8'
services:
  spigot:
    build: .
    container_name: spigot188
    ports:
      - "25565:25565"
    volumes:
      - ./:/workspace
    working_dir: /workspace
    environment:
      - EULA=TRUE
    stdin_open: true
    tty: true
    restart: unless-stopped
